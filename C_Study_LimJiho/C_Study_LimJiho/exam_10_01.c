//예제 10-1_포인터 변수의 선언 및 초기화
/*
#include <stdio.h>

int main(void) {
	char cnum = 5;
	int inum = 999;
	double dnum = 3.14;

	char* cptr = &cnum;
	int* iptr = &inum;
	double* dptr = &dnum;

	//포인터 변수의 출력에는 형식 문자 %p를 사용합니다.
	printf("%p\n", cptr);
	printf("%p\n", iptr);
	printf("%p\n", dptr);

	return 0;
}*/